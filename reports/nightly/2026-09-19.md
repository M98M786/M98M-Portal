# Nightly portal audit — 2026-09-19

_Run 03:32–04:10 PKT (22:32–23:10 UTC 18 Sept). Owner session, Management. Build 20260918-1525,
engine deployment `36e26d6e`._

## Verdict

Machinery is healthy — every cron clean, zero sync errors, zero truth failures — but three things
were telling the office something untrue, two are fixed and live, and the third is a books gap on
Amna Baji that only a person can close.

## Findings

### 1. Buyer auto-messages have stopped entirely, and the CS desk was told they were live — HIGH

`portal_config.automsg_live` is **off**, the `auto_msgs` template table is **empty for every
account**, and `automsg_queue` holds **zero rows**. Nothing is queued, nothing is sent, nothing is
even recorded in shadow. On 12 Sept this pipeline was doing purchase→message in about seven
minutes; some time in the last week the templates were deleted and the switch turned off.

Worse, the Auto-messages panel on the CS desk has been showing a green **"LIVE — enabled triggers
SEND"** pill the whole time. `autoMsgConfig` reported `live` from `ctx.env.AUTOMSG_LIVE`, the env
var that commit 2849a91 retired when `portal_config.automsg_live` became the sender's single
authority — and that var is still armed `true` in production. So the page read one switch while
the sender obeyed another.

The pill is fixed (below). **The switch and the templates are deliberately untouched** — restoring
them resumes messages to real buyers, which is Hasib's call, not an overnight one.

**Ask:** was this a deliberate pause, or did the templates get wiped? If it was deliberate, nothing
to do. If not, the templates need rebuilding before the switch goes back on.

### 2. Amna Baji has no day tab for 17 September (and her 18th is not written either) — HIGH

`Claude Amna Saif Sales Analysis Sheet` runs `1st September` … `16th September 2026` and then
stops. All five other accounts have their 17th. Her sales that day were real — £426.29 sold,
£59.03 actual profit, ~55 orders — but there is no book behind them, so:

- the truth suite records 17 Sept as STALE for her (it recomputes from the sheet and finds 0 rows);
- her VAT for that day has no sheet basis at all;
- the money-book mirror (`sheet_rows`) has no Amna Baji row for 17 Sept.

No account has an 18 Sept tab yet, which is normal — the 18th gets written on the next shift. The
17th being missing on one account is not.

**Ask:** whoever writes Amna Baji's books needs to add 17th and 18th September. I have not created
them — day tabs are money on a business sheet.

### 3. Staff email hardcoded in a public repo — MEDIUM

`engine/worker.js` carries `m98mfour@gmail.com` twice (a default assignee at :1809 and an archive
permission gate at :12359). `scripts/rl-scan.sh` is designed to catch exactly this and **does** flag
it — on HEAD, before my change — yet the code ships to a repo that is public
(`github.com/M98M786/M98M-Portal` answers unauthenticated). Not introduced tonight; long-standing.

Not fixed tonight on purpose: :12359 gates who can see the listings archive, and getting that wrong
at 4am locks Zain out of his own desk. It wants a `portal_config` lookup and five minutes of
daylight. The address is already public, so an hour's delay costs nothing.

### 4. `truthAlertSweep` is scheduled by nothing — LOW (latent)

The function exists and is runnable, but appears in no cron slot; last run was 1 Sept, by hand. I
ran it tonight: it closed nothing, because the oldest unresolved alert letter is only ~24h old, so
the backlog it exists to clear is being handled some other way. Harmless today, a trap the day
alert volume rises. Watchlist rather than a change — I would rather not spend a structural change
on a job with nothing to do.

## Fixed tonight

| Fix | Where | Commit / deploy |
|---|---|---|
| VAT_TO_HMRC was never re-verified after its one-night window | `engine/worker.js` `truthTier3` | `c175702` · engine `36e26d6e` |
| Auto-messages panel reported the retired env var as the live switch | `engine/worker.js` `autoMsgConfig` | same |

**The VAT gap.** `VAT_TO_HMRC` was only ever checked by `truthTier1`, on the single night a day was
"yesterday". If an account's day tab had not reached `sheet_rows` by 03:00 PKT, the check recorded
STALE — and nothing ever came back for it, because `truthTier3` re-reads exactly those rows for the
last six closed days but only re-checked `ACTUAL_PROFIT`. That asymmetry is the whole story behind
the board showing 57 stale VAT scopes against 18 stale profit ones. Amna Baji 15 Sept is the clean
example: 30 rows sitting in `sheet_rows`, profit PASS, VAT still reporting "0 row(s)". For a
VAT-registered business that is 57 days of VAT nobody had verified.

Tier 3 now emits `VAT_TO_HMRC` from the same loop, honouring the tab's own totals row the same way
profit does.

**Verified live** on the Truth Check page after deploy:

| | before | after |
|---|---|---|
| PASS | 260 | **272** |
| STALE | 75 | **63** |
| FAIL | 0 | **0** |

Twelve VAT scopes that would have stayed unverified forever now pass, deltas at £0.00.

**The auto-message pill** now reads the same `portal_config` row the sender reads, and reports the
env var separately as `env_force` instead of letting it speak for the switch. Verified live:
`autoMsgConfig` returns `live:false, env_force:true` (was `live:true`). The CS desk will show
"SHADOW — records, never sends", which is the truth.

Deploy notes: PUT from the dashboard pane with `X-Cross-Site-Security: dash` and `keep_bindings`;
all five secrets survived (`SYNC_KEY`, `EBAY_APP_ID`, `EBAY_CERT_ID`, `EBAY_DEV_ID`, `EBAY_RU_NAME`)
and a post-deploy `orderSync` came back clean across all six accounts, so eBay auth is intact.

## Staff working

**Listing** — 174 open, **106 overdue**, oldest 31 Aug. `m98meleven` is carrying 121 of the 174.
Split 95 `listing_new` / 79 `listing_revision`. Against the 15+5/day and 72h benchmarks this is a
capacity problem, not a diligence one — one person cannot clear 121.

**Orders** — 116 open, **110 overdue**, every one a `supplier_add`, oldest 5 Sept, split evenly
across `m98msix` / `m98mnine` / `m98meight` (39/38/39). A queue that is 95% overdue and evenly
spread is a queue nobody has been told to work.

**Advertising** — 66 open, **56 overdue**, oldest 12 Sept. `m98mfour` holds 60, of which 50 are
`loss_review`. Nine `campaign_set` tasks were submitted at 00:41 last night and are waiting on
approval (longest lag 8h, none overdue yet).

**Hunting** — 38 open, **29 overdue**, all on `m98mfive`: 30 `sourcing_link` + 8 `hunt_revision`.
Against the 30 hunts/day benchmark the backlog is roughly one day's output, but it is ageing.

**CS** — 0 open, 0 overdue. The under-24h benchmark is being met. Clean.

**Listing ladder** — the revision desks are not moving:
- 72-hour desk: **19 queued**, oldest gone live **9 Sept** — ten days in a 72-hour window.
- 10-day desk: **41 queued**, oldest gone live **24 Aug** — twenty-six days.
- 20-day desk and the final desk: empty, parked 0, video queue 0 — nothing is reaching them,
  because nothing is getting through the two desks above.

**Attendance, 18 Sept** — 14 people: 4 present, 1 still working, 7 absent.
- **Rana Noman** clocked in 21:57 and never clocked out.
- **Muhammad Sajjawal** shows 01:19 → 21:18, **19h 59m**. That is a missed clock-out from the 17th
  paired with the 18th's clock-out; it alone is 20 of the team's 46.68 recorded hours, so the daily
  total is overstated.
- Reports missed: Umair, Husnain, Zeeshan, Yousaf, Umar, Hasib.
- **Yousaf** has 0 present days in 26 working days; **Umair** 3 in 30; **Zain** 3 in 30 — yet Zain's
  desk submitted nine approvals at 00:41. Work is landing outside recorded attendance, so the
  attendance numbers are not describing what the team is actually doing.

## Numbers checked

- **Truth suite** — `truthTier1` 18.6s, `truthTier3` 20.5s/28.8s, both clean. 272 PASS / 0 FAIL.
- **Crons** — `orderSync` 22.7s, `csSync` 40.4s, `feedbackSync` 17.5s, `autoMsgScan` 0.6s,
  `autoMsgSend` 1.4s, `ladderWatch` 2.2s, `trackingBackfill` 2.0s. **Zero `last_error` on any
  `sync_state` row, across every job and every account.**
- **`runZeroSalesSweep`** (the known 100%-daily-failure defect) — the per-account try/catch and the
  error-logging wrapper are in `build/Code.gs` and the trigger's `sync_state` shows no error. The
  fix from a previous session is holding.
- **Daily report** — 17 Sept complete for all six accounts, profit/OE/cost all non-zero:
  ABRT £309.79 sold / £22.42 actual · Amna £426.29 / £59.03 · Azhar £243.40 / £8.02 ·
  HAFIZA £236.01 / £31.96 · Saif £476.07 / £66.52 · Hasib £392.70 / £39.40. The 18th shows only a
  live-order trickle, which is correct — its book lands at the 02:10 UTC rollup.
- **Signals** — 81 detected, 60 shown, newest dated 18 Sept. Fresh.
- **Account health** — all six answering; 7-day revenue £1,745–£3,292; `loss_items` 0 everywhere.
  Sir Hasib is running 39 of 48 campaigns, HAFIZA 41 of 44 — worth an advertising look.
- **Dispatch** — late now **0**, refunded-never-sent **0**, pending payment **0**, awaiting 319,
  shipped 7d 1161. Fulfillment benchmark met.
- **Pages** — `#tasks`, `#orders`, `#signals`, `#dispatch`, `#truthCheck` all render; no
  "could not be loaded", no NaN, no broken images (0 of 42 on signals), no app console errors.
  Only Google Sign-In FedCM noise. (The checklist's `#dispatchToday` is not a route — the page is
  `#dispatch`; an unknown hash leaves the previous view up.)

## Watchlist for tomorrow

1. **Amna Baji 17 + 18 Sept day tabs.** Until they exist, her VAT and profit for two days are
   unverifiable. Re-check the truth board: they should flip STALE → PASS on their own once the tabs
   are written, now that tier 3 re-checks VAT.
2. **Did the auto-message templates go on purpose?** If not, this is a silent loss of every buyer
   touch since roughly 12 Sept.
3. **The two revision desks.** 19 items ten days past their 72-hour window and 41 items twenty-six
   days past go-live is the ladder backing up from the front.
4. **`m98meleven` at 121 open listing tasks** and the 110 untouched `supplier_add` orders — both
   look like unassigned capacity rather than slow work.
5. **Attendance is not describing reality.** Two broken clock-outs tonight, and people submitting
   work on days they are marked absent.
6. `truthAlertSweep` scheduled by nothing; `rollupsWide` last ran 29 Aug. Neither is hurting
   anything today.
7. The hardcoded staff address in `worker.js` — a ten-minute daylight fix.

---

# Second run — 05:00–05:40 PKT

_A scheduled 06:15 run firing early, on the same date as the 03:32 audit above. Rather than repeat
that pass an hour later, this run re-verified the live state, finished the VAT work the first run
started, and worked the checks the first run could not reach. Engine deployments `2d6151a6` then
`941bbec2`._

## Verdict

Nothing is broken this hour: zero truth failures, zero `last_error` on any cron, all seven named
jobs clean. Two silent holes closed — 116 days of VAT for a VAT-registered business verified for
the first time, and seven tracking pushes eBay rejected made visible after weeks of being
invisible by design.

## Findings

### 1. The VAT fix could never reach 1–11 September — FIXED

Last night's tier-3 change made VAT re-check itself, and the board went 260 → 272 PASS. But tier 3
only ever looks at days −2…−7 (`for (let k = 2; k <= 7; k++)`). Every day older than that had
already left the window, so the new check could never run on them. The board showed it plainly:

| | 4–11 Sept |
|---|---|
| `ACTUAL_PROFIT` | PASS (except Azhar Bhai) |
| `VAT_TO_HMRC` | STALE, "0 row(s)" |

Profit passing on the same account and day proves the sheet rows were there all along — only VAT
was never re-read. And `validation_runs` is pruned at 14 days, so those STALE rows were about to be
deleted: the gap was on course to erase its own evidence and leave the board looking clean.

**Fixed and verified live.** The per-day check moved out of `truthTier3` unchanged into
`truthRecheckDay`, and a new mgmt-gated `truthBackfill` re-runs it over an explicit window. Two
calls covered 1–11 Sept:

| window | written | PASS | FAIL | STALE |
|---|---|---|---|---|
| 5–11 Sept | 84 | 76 | **0** | 8 |
| 1–4 Sept | 48 | 40 | **0** | 8 |

**Zero failures — every VAT figure was correct all along, just unverified.** Board after:
**309 PASS / 0 FAIL / 32 STALE** (from 265 / 0 / 58).

All 16 remaining stale rows in that window are Azhar Bhai, 30 Aug – 8 Sept, and they are **not** a
data hole: the engine reports £0 for him on every one of those days, matching the empty book. That
is the dormant stretch before he came back on 9 Sept. Genuinely nothing to verify.

The backfill is capped at 7 days a call and refuses any day tier 1 still owns, so it cannot be
pointed at a day whose book is still filling.

### 2. Seven tracking numbers eBay rejected, invisible to every sweep since 25 August — MADE VISIBLE

`trackings` holds 7 rows with `push_status` `FAIL:400` (5) and `FAIL:500` (2), from 25 Aug to
11 Sept. We hold a tracking number for each; the buyer's eBay order shows none.

Nothing was ever going to retry them. `trackingBackfill` selects orders
`WHERE NOT EXISTS (… t.tracking != '')` — the failed row **keeps its tracking number**, so the
filter reads it as "already has tracking" and skips it permanently. No other sweep looks at
`push_status` at all. A failed push is not retried, not alerted, not counted anywhere.

**Fixed the visibility, not the pushes.** Tier 1 now emits `TRACKING_PUSH_FAILED`, confirmed live
on the board:

> 7 tracking push(es) rejected by eBay and never retried, oldest 2026-08-25 14:26:16 — the order
> shows no tracking to the buyer

Deliberately `INFO`, not `FAIL`: the number is not wrong, the push failed. `FAIL` would fire the
A16 alert letter every 15 minutes for a condition nobody can clear automatically.

**Not re-pushed on purpose.** Writing tracking onto a buyer's eBay order is an outward-facing act
that can trigger eBay's own buyer notification — on orders 8 to 25 days old, with auto-messages
under an owner-ordered halt. That is Hasib's call. The seven order IDs are listed under Numbers.

### 3. Amna Baji still has no 17 September book — HIGH, unchanged

Re-confirmed from two directions: `sheet_rows` has no Amna Baji row for 17 Sept (all five other
accounts do), and her 17 Sept profit and VAT both re-read as 0 rows in tonight's backfill window.
Her sales that day were real — £426.29 sold, £59.03 actual. Still a person's job; see run 1.

### 4. `runZeroSalesSweep` — could NOT be verified this run

The first run called it healthy on the grounds that the try/catch wrapper is in `build/Code.gs` and
no `sync_state` row shows an error. That evidence does not hold: the sweep is an **Apps Script
trigger**, and AS triggers do not write engine `sync_state` at all. Its failures go to
`ACTIVITY_LOG` as `ERROR:zeroSales`, which needs either the AS editor or the sync key —
`asRunJob` is `auth: 'sync'` and the key never enters a browser, and `activityTail` is a *runnable*,
not an `api()` action (calling it returns "unknown action").

Indirect check, inconclusive: no `listing_revision` tasks were created on 18 Sept. That is not
proof of failure — the sweep suppresses a repeat while an open task exists on the same item, and
79 `listing_revision` tasks are already open.

**Route for tomorrow:** read `ACTIVITY_LOG` in the Portal DB via the sheet htmlview (the trick used
on 18 Sept for the signals freeze) and grep `ERROR:zeroSales` around 07:00 PKT. Reported as
unverified rather than green.

### 5. Attendance is still overstating hours — MEDIUM, unchanged

18 Sept totals 46.68 hours across 14 people, and **20 of those hours are one broken record**:
Muhammad Sajjawal shows 01:19 → 21:18, 19h 59m — the 17th's missing clock-out paired with the
18th's. Rana Noman clocked in 21:57 and is still "working" at 05:30, 7h 33m later.

Not fixed: correcting it means editing staff attendance records, and a display-side guard (treating
an implausible span as a missed clock-out rather than counting it) is an Apps Script change that
would alter the hours people are measured on. Both want daylight and Hasib's word.

Three people the first run listed as never clocking out — Zeeshan, Daniyal, Umar — did clock out at
00:09–00:12, after that audit ran. They were mid-shift, not broken.

## Fixed this run

| Fix | Where | Commit / deploy |
|---|---|---|
| VAT verified for 1–11 Sept; `truthBackfill` added so a closed window can be re-checked | `engine/worker.js` `truthRecheckDay`, `truthBackfill` | `5b8bba0` · engine `2d6151a6` |
| Tracking pushes eBay rejected now counted on the truth board | `engine/worker.js` `truthTier1` | `9eb020f` · engine `941bbec2` |

Both deploys: dash-pane multipart PUT with `X-Cross-Site-Security: dash` and `keep_bindings`.
Bindings re-read after each — all five secrets (`SYNC_KEY`, `EBAY_APP_ID`, `EBAY_CERT_ID`,
`EBAY_DEV_ID`, `EBAY_RU_NAME`) plus `DB` and `HOT` intact, and a post-deploy `orderSync` came back
clean on all six accounts, so eBay auth survived both.

## Staff working

Unchanged from run 1 in substance; the queues did not move overnight, as expected at this hour.

- **Listing** — 174 open, **107 overdue**, oldest 31 Aug. `m98meleven` carries 121.
- **Orders** — 116 open, **110 overdue**, all `supplier_add`, evenly split 39/38/39 across
  `m98msix` / `m98mnine` / `m98meight`. **Corroborated independently tonight:** the
  `order_processing` table is completely empty — 0 rows, not one PENDING. That is the table the
  portal writes when someone records a Cost or AliExpress order number. Nobody is using the box.
  The queue is not slow, it is unworked.
- **Advertising** — 66 open, **56 overdue**. `m98mfour` holds 60, 50 of them `loss_review`.
- **Hunting** — 38 open, **30 overdue**, all on `m98mfive`.
- **CS** — 0 open, 0 overdue. Benchmark met.
- **Listing ladder** — still stalled, unchanged: 72-hour desk **19 queued**, oldest due 12 Sept
  (gone live 9 Sept); 10-day desk **41 queued**, oldest due **3 Sept**, gone live 24 Aug — 16 days
  past its own decision point. 20-day desk and final desk empty, parked 0, video 0. The r72 queue
  by lister: `m98mone` 10, `m98meleven` 8, `m98mthree` 1. Nothing is reaching the lower desks
  because nothing clears the upper two.
- **Approvals** — 0 pending. The nine `campaign_set` tasks waiting at run 1 have been approved.

## Numbers checked

- **Truth** — `truthTier1` 16.9s then 22.8s, `truthTier3` 27.6s, both `last_error` empty.
  **309 PASS / 0 FAIL / 32 STALE**; 132 scopes written by the backfill, 116 PASS, 0 FAIL.
- **Crons** — `orderSync` 25.2s ✓, `feedbackSync` 18.0s ✓, `autoMsgScan` 3.0s ✓, `autoMsgSend` 1.3s ✓,
  `ladderWatch` 2.2s ✓, `trackingBackfill` 1.9s ✓, `csSync` completed server-side 00:23 UTC ✓
  (the client aborted at 45s — the tool's limit, not the job's; verified in `sync_state`).
  **Zero `last_error` on any row of `sync_state`, fleet-wide.**
- **Daily report** — 17 Sept complete, six of six, all non-zero: AZHAR ABRT £309.79 sold /
  £22.42 actual · Amna £426.29 / £59.03 · Azhar Bhai £243.40 / £8.02 · HAFIZA £236.01 / £31.96 ·
  Saif £476.07 / £66.52 · Sir Hasib £392.70 / £39.40. 18 Sept shows a live-order trickle only,
  correct — its book lands at the 02:10 UTC rollup.
- **Day-tab costs** — 16 and 17 Sept checked per account against
  `Total AliExpress Cost incl VAT`: **0 rows missing cost** on either day, every account.
- **Signals** — 60 shown of 81, newest 18 Sept. Fresh.
- **Auto-messages** — `automsg_live: off`, `env_force: true`, queue 0 rows, templates empty.
  **Left exactly as found.** Run 1's pill fix survives both deploys (`autoMsgConfig` → `live:false`).
- **Dispatch** — late now **0**, refunded-never-sent **0**, pending payment **0**, due within 3 days 1,
  awaiting 325, shipped 7d 1157, cancelled 30d 82.
- **Failed tracking pushes** — `23-15124-83732` (RoyalMail, 11 Sept), `24-15110-42179` (Other,
  5 Sept), `25-15090-83722` (DHL, 4 Sept), `16-15089-02169` (DHL, 2 Sept), `26-14949-41291`
  (DHL, 28 Aug), `01-15088-47288` (Other, 25 Aug), `21-15002-71974` (DHL, 25 Aug).
- **Pages** — `#tasks`, `#orders`, `#signals`, `#dispatch`, `#truthCheck` all render with real data.
  No app console errors; only Google Sign-In FedCM noise. **`#dispatch` needs ~10s to populate** —
  at 5s it shows its header and nothing else, which reads as broken. Worth a spinner.

## Watchlist for tomorrow

1. **Amna Baji's 17th and 18th.** Now that both tier 3 and the backfill re-check VAT, they flip to
   PASS on their own the moment the tabs are written.
2. **The seven failed tracking pushes** — Hasib's decision whether to re-push to eBay. Watch
   `TRACKING_PUSH_FAILED` on the truth board: if it climbs above 7, pushes are failing *now* and
   that is a live problem, not a backlog.
3. **`runZeroSalesSweep` still unverified** — read `ACTIVITY_LOG` for `ERROR:zeroSales` near 07:00.
4. **The two revision desks**, unmoved: 19 items and 41 items, the older set 16 days past due.
5. **`order_processing` empty** alongside 110 overdue `supplier_add` tasks — the same fact twice.
6. Carried from run 1 and untouched: the hardcoded staff address in `worker.js` (`rl-scan.sh` still
   flags it on HEAD), `truthAlertSweep` in no cron slot, `rollupsWide` last run 29 Aug.

---

# Third run — 20:36–21:35 PKT

_The scheduled 06:15 run fired at 20:36 PKT instead, on the same date as the two audits above. That
put the audit inside the working shift rather than the quiet window, which is worth something the
other two runs could not get: the queues could be read at the end of a full working day and
compared against where they stood at 05:30. Engine deployment `82b8bca5`._

## Verdict

Every machine is green — 0 FAIL on the truth board, zero `last_error` on any of the 78 cron jobs —
but two accounts sold all day on 18 September into a book nobody wrote, and a complete working
shift moved the overdue queues by almost nothing.

## Findings

### 1. AZHAR ABRT and Azhar Bhai have no 18 September book — HIGH

Both workbooks stop at **17th September 2026**. Confirmed from three directions: `sheet_rows` holds
18 Sept for the other four accounts and nothing for these two; the truth board records both days
STALE at "0 row(s)"; and `wbInspect` lists the tabs, which simply end at the 17th.

Their sales that day were real, and the engine knows it from eBay independently of any sheet:

| account | eBay orders 18 Sept | sold | actual |
|---|---|---|---|
| AZHAR ABRT | 22 | £197.75 | £30.28 |
| Azhar Bhai | 37 | £369.20 | £29.21 |

So profit, VAT and margin for those two account-days rest on nothing. This is the **third
occurrence in three days** — Amna Baji's 17th was the first, and it is now written and passing, as
are her 18th and the other four accounts'. The pattern is the finding: books get missed, and
nothing said so.

**Not fixed by me — day tabs are money on a business sheet.** Whoever writes these two books needs
to add 18 September (and 19 September on the next shift). Once written they flip to PASS on their
own, now that both tier 3 and the backfill re-check VAT.

**What I did fix is that nobody was told.** See below.

### 2. A full working shift barely moved the overdue queues — HIGH (staff working)

Same feed, 05:30 PKT versus 21:22 PKT today:

| department | open 05:30 → 21:22 | overdue 05:30 → 21:22 |
|---|---|---|
| Listing | 174 → **169** | 107 → **103** |
| Orders | 116 → **121** | 110 → **110** |
| Advertising | 66 → **73** | 56 → **57** |
| Hunting | 38 → **39** | 30 → **33** |
| CS | 0 → 0 | 0 → 0 |

Listing cleared four overdue items in a day against a 15+5/day benchmark. **Orders moved zero** —
110 overdue `supplier_add` at the start of the shift and 110 at the end, still split evenly
39/38/39 across `m98msix` / `m98mnine` / `m98meight`, oldest 5 Sept. Advertising and Hunting both
finished the day with *more* overdue work than they started with. CS remains genuinely clean.

`processWatch` reports `0 unprocessed >1 biz day` and there are no stuck `PENDING` rows, so the
machinery behind the Orders queue is idle and waiting — the queue is unworked, not blocked.

### 3. The listing ladder desks have stopped deciding — HIGH (staff working)

- **72-hour desk: 19 queued**, every one `QUEUED`, oldest went live 9 Sept and was due 12 Sept.
  The last decision recorded at this stage was **11 September, eight days ago**, by `m98mfour`.
  By lister: `m98mone` 10, `m98meleven` 8, `m98mthree` 1.
- **10-day desk: 41 queued**, oldest live 24 Aug, **due 3 September — sixteen days past**.
  `ladder_decisions` holds **no R10 or R20 decision at all**, ever.
- 20-day desk 0, final desk 0, parked 0, video 0 — nothing reaches them because nothing clears the
  two desks above.

Decided work does exist further back (26 in Tier 1, 43 in Tier 2), so the desks worked once and
stopped.

### 4. Attendance still is not describing the day — MEDIUM (unchanged)

18 September closed at 4 present / 9 absent / 1 never concluded. **Rana Noman** clocked in at 21:57
on the 18th and that record has still never closed. **Muhammad Sajjawal's** 01:19 → 21:18 span
(19h 59m) stands and is still 20 of the day's 46.68 recorded hours. Today, all six people who
clocked in are flagged late and none had clocked out at 21:22.

The roster also carries **two separate rows both named "Hasib"** among its 14 people, which makes
any per-person total ambiguous.

Unchanged from last night on purpose: correcting records or adding a display-side implausible-span
guard both change the hours people are measured on, and want daylight and Hasib's word.

### 5. `runZeroSalesSweep` — still not verifiable from the browser

The route last night proposed (read `ACTIVITY_LOG` for `ERROR:zeroSales`) does not work from here
after all. `asInspect` is hardwired to relay only `wbInspect`, and `wbInspect` returns a tab's
*first* N rows plus the *last 4* — on an append-only log that means the newest four entries, which
cannot reach back to a 07:00 run. Reading it needs the Portal DB spreadsheet id (held in an Apps
Script property, not in the repo) or the AS editor.

Reported as unverified for the second night rather than green. The durable fix is to make the sweep
write `sync_state` like every engine job does, so it stops being invisible — an Apps Script deploy,
which is the riskiest kind and not one to spend on a shift evening.

### 6. Noted, not defects

- **`TOTALS_VS_ITEMS` — 41 INFO rows is by design, not a fault.** The tab's GRAND TOTAL row and the
  sum of its item rows disagree on almost every account-day, by £13–£45 (Sir Hasib 18 Sept: totals
  row £59.35, items £82.06). Commit `45c8fac` made the totals row the headline deliberately — it is
  the owner's number — and surfaces the gap as INFO, with the report agent owning the difference.
  Worth knowing only because these 41 rows are what the board's INFO count is mostly made of.
- **Failed tracking pushes still exactly 7**, oldest 25 Aug. Not climbing, so nothing is failing
  now; the backlog is still Hasib's call to re-push or leave.
- **Auto-messages left exactly as found**: `automsg_live: off`, `env_force: true`, queue 0 rows,
  templates empty.
- `truthAlertSweep` still sits in no cron slot (its last run is the one I did by hand last night).
  `rollupsWide` last ran 29 Aug. Neither is hurting anything.

## Fixed tonight

| Fix | Where | Commit / deploy |
|---|---|---|
| A day book that was never written no longer reads like a quiet day | `engine/worker.js` `bookMissingRow`, `truthTier1`, `truthRecheckDay` | `f7cbdb9` · engine `82b8bca5` |

**Why it was invisible.** Both sides of the money check read `sheet_rows`. When the book is missing,
the shown number and the recomputed number are both zero, the delta is zero, and the row records a
mute `STALE — 0 rows` that looks exactly like a dormant account. Azhar Bhai's genuinely empty
30 Aug – 8 Sept stretch produces the identical row. That is why Amna Baji's 17th sat there until a
person went looking, and why these two would have too.

The `orders` table is the engine's own eBay truth and owes the sheets nothing, so it separates the
two cases: **no sheet rows AND real orders** means the book was never written; no rows and no orders
means nothing happened. `metricMoney` already returned that count in `ROWS_COVERAGE.orders` — it was
being computed and thrown away.

`BOOK_MISSING` now carries the order count as its evidence, emitted from tier 1 for yesterday and
from the tier 3 re-check for the closed days behind it. **Verified live:**

> `AZHAR ABRT:2026-09-18` — no day tab for 2026-09-18 — eBay shows **22 order(s)** that day, the
> book has 0 row(s); profit and VAT for the day rest on nothing until someone writes it
>
> `Azhar Bhai:2026-09-18` — same, **37 order(s)**

Deliberately `INFO`, not `FAIL`, on last night's precedent: the number is not wrong, it is absent,
and `FAIL` fires the A16 letter every fifteen minutes at a condition only a person writing the tab
can clear.

**And a bell, because the board alone was not enough** — a STALE money row sat on Truth Check for
days and only an audit found it. Management now gets one letter a day naming the accounts and their
order counts. It is guarded by a `sync_state` day-cursor, the same guard `darkAccountWatch` uses:
`notif_live` has **no unique index on `ref`**, so `INSERT OR IGNORE` does not dedupe and the cursor
is the only thing that does. **Verified: three `truthTier1` runs, exactly one bell**, delivered and
readable in `pollEngine`.

Checked against false positives before deploying: Azhar Bhai's dormant 28 Aug – 8 Sept days report
£0 sold and 0 orders, so they stay plain STALE and raise nothing. After the deploy, tier 3 re-ran
over 12–17 Sept and produced no `BOOK_MISSING` rows at all. The board still reads **0 FAIL**.

Deploy: dash-pane multipart PUT with `X-Cross-Site-Security: dash` and `keep_bindings`. Bindings
re-read after — all five secrets (`SYNC_KEY`, `EBAY_APP_ID`, `EBAY_CERT_ID`, `EBAY_DEV_ID`,
`EBAY_RU_NAME`) plus `DB` and `HOT` intact, and `orderSync` came back clean on all six accounts.

## Staff working

- **Listing** — 169 open, 103 overdue, oldest 31 Aug. `m98meleven` still carries 116 of 169;
  `m98mone` 31, `m98mthree` 22. Split 90 `listing_new` / 79 `listing_revision`.
- **Orders** — 121 open, 110 overdue, all `supplier_add`, oldest 5 Sept. Zero overdue cleared today.
- **Advertising** — 73 open, 57 overdue. `m98mfour` holds 67, of which 52 `loss_review`; 6 keyword
  tasks sit with Zain. Six `campaign_set` approvals were submitted 21:12–21:26 tonight — fresh, not
  a backlog, and the same end-of-shift burst pattern as last night's nine at 00:41.
- **Hunting** — 39 open, 33 overdue, all `m98mfive`: 31 `sourcing_link` + 8 `hunt_revision`.
- **CS** — 0 open, 0 overdue. The under-24h benchmark is being met.
- **Ladder** — see finding 3.
- **Attendance** — see finding 4.

## Numbers checked

- **Truth** — `truthTier1` 18.4s / 21.7s, `truthTier3` 26.8s, both `last_error` empty.
  **319 PASS / 0 FAIL / 22 STALE / 44 INFO.** Of the 22 STALE, 20 are Azhar Bhai's genuinely empty
  dormant days and the other 2 are the missing 18 Sept books, now also named by `BOOK_MISSING`.
- **Crons** — all seven named jobs run and clean: `orderSync` 26.3s, `feedbackSync` 16.4s,
  `csSync` (completed server-side 16:26, all six accounts — the client aborts at 45s, the tool's
  limit not the job's), `autoMsgScan` 2.8s, `autoMsgSend` ✓, `ladderWatch` 2.1s,
  `trackingBackfill` 3.7s, plus `processWatch` 1.4s. **Zero `last_error` on all 274 `sync_state`
  rows, across all 78 jobs.**
- **Daily report** — 18 Sept complete for all six accounts, every figure non-zero: ABRT £197.75
  sold / £30.28 actual · Amna £378.56 / £57.03 · Azhar Bhai £369.20 / £29.21 · HAFIZA £262.21 /
  £18.94 · Saif £494.15 / £65.04 · Sir Hasib £463.85 / £37.75. (For two of those six there is no
  book behind the number — finding 1.)
- **Day-tab costs, 18 Sept** — every account that has a book checked row by row against
  `Total AliExpress Cost incl VAT`: Amna 23 rows, HAFIZA 16, Saif 26, Sir Hasib 19 — **0 rows
  missing cost** anywhere.
- **Signals** — 91 detected, 60 shown, newest dated 18 Sept. Fresh.
- **Account health** — all six answering; 7-day revenue £1,749–£2,747+; `loss_items` **0 on every
  account**; campaigns running 32/32 to 43/46.
- **Dispatch** — late now **0**, refunded-never-sent **0**, pending payment **0**, due within 3 days
  1, awaiting 223, shipped 7d 1295, cancelled 30d 82. Invariant holds (224 open = late + due +
  awaiting).
- **Approvals** — 6 pending, all `campaign_set`, all submitted within the last 20 minutes.
- **Auto-messages** — `automsg_live: off`, `env_force: true`, queue 0, templates empty. Untouched.
- **Pages** — `#tasks`, `#orders`, `#signals`, `#dispatch` all render with real data. **Zero console
  errors, zero broken images** (0 of 19 on orders, 0 of 44 on signals), no "could not be loaded",
  no NaN. `#dispatch` still needs ~12s to populate and shows a bare header until then.

## Watchlist for tomorrow

1. **AZHAR ABRT and Azhar Bhai, 18 and 19 September.** The bell now says so every day until the
   tabs exist. If `BOOK_MISSING` appears for a *third* account, the books process itself is the
   problem, not the individuals.
2. **Orders: 110 overdue, zero cleared in a full shift.** This is the single clearest staff-working
   signal in tonight's data and it has now held for three consecutive audits.
3. **The ladder desks** — no 72-hour decision since 11 Sept, no 10-day decision ever. 41 items are
   16+ days past their own decision point.
4. **`runZeroSalesSweep`** — needs the AS editor or the Portal DB id; the browser route is a dead
   end. Worth making it write `sync_state` when there is daylight for an Apps Script deploy.
5. Carried and untouched: the hardcoded staff address in `worker.js`, `truthAlertSweep` in no cron
   slot, `rollupsWide` last run 29 Aug, the 7 failed tracking pushes, the duplicate "Hasib" roster row.
